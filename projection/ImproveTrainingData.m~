function [ X_bridge ] = ImproveTrainingData( X, gpr )
    N = 5000;
    [ X_bridge ] = BridgeSampling( X, 100 );
    Y_bridge = gpr.predict(X_bridge);
    Y_bridge_categ = TaskCategorisationHammeringNail(Y_bridge);
    num_min_categ = size(Y_bridge_categ(Y_bridge_categ==min(Y_bridge_categ)),1)
    X_balanced = zeros(N,size(X,2));
    X_balanced(1:1250,:) = X_bridge(randsample(
end

